#include <bits/stdc++.h>

using namespace std;

void solve() { }

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int test_cnt = 1;
    cin >> test_cnt;

    while (test_cnt--) {
        solve();
    }
}
